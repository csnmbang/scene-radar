[project]
name = "scene-radar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.120.0",
    "curl-cffi>=0.15.0",
    "duckdb>=1.5.5",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.2",
    "rapidfuzz>=3.14.5",
    "selectolax>=0.4.11",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
